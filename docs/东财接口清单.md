# 东财接口清单 — 东方财富接口爬取

> 数据源选型：**东方财富接口爬取**（用户确认）。本文件列出所需接口、用途、字段与风险对策。
> 版本：v0.3（池子/资金流/概念板块已实现于 `data/eastmoney_pool.py`；K 线与实时行情见 v0.2 `data/eastmoney.py` / `data/sina.py`）

---

## ⚠️ 总原则

1. **东财接口无正式文档**，接口路径与字段可能随时变更。实现时必须：字段映射集中在单一模块（见 `docs/数据结构.md`），所有返回值先校验再落盘。
2. 加 `User-Agent` 伪装浏览器、控制请求频率（≥1s/请求）、失败指数退避重试、**主机轮换**（`push2 / 1./2.push2 / push2delay`）、按日缓存避免重复请求。
3. 数据仅在收盘后采集（A 股 15:00 后数据稳定）；盘中跑 `review` 数据不完整。

## 一、核心接口清单

| # | 接口 | 用途 | 关键字段 | 频率 | 状态 |
|---|---|---|---|---|---|
| 0 | **东财 K 线** `push2his.eastmoney.com/api/qt/stock/kline/get` | 单股历史 K 线（日/周/月） | 开/收/高/低/量/额/涨跌幅/换手 | 按需 | ✅ v0.2 已实现（`data/eastmoney.py`） |
| — | **新浪实时行情** `hq.sinajs.cn/list=` | 一组股票实时快照（收盘后即当日收盘数据） | 开/收/高/低/昨收/量/额/买一/卖一 | 按需 | ✅ v0.2 已实现（`data/sina.py`，gbk） |
| 1 | **涨停池** `push2ex.eastmoney.com/getTopicZTPool` | 当日涨停股清单 | `c`代码 `n`名称 `lbc`连板数 `fbt`首封 `zbc`炸板次数 `fund`封单 `hs`换手 `amount`成交额 `hybk`行业 | 每日 1 次 | ✅ v0.3 已实现，**支持历史日期** |
| 2 | **炸板池** `getTopicZBPool` | 当日炸板股清单 | `c/n` `zbc`炸板次数 `fbt`首封 `zdp`收盘涨幅 `hybk`行业 | 每日 1 次 | ✅ v0.3 已实现 |
| 3 | **跌停池** `getTopicDTPool` | 跌停股（情绪参考） | `c/n` `zdp` | 每日 1 次 | ✅ v0.3 已实现 |
| 4 | **连板股池** `getTopicLBPool` | 连板（≥2 板）细分 | 连板数/晋级状态 | 每日 1 次 | ⬜ 未用（涨停池含连板数，够用） |
| 5 | **题材/概念板块行情** `clist fs=m:90+t:2` | 各概念板块涨幅、主力净流入 | `f12/f14/f3/f62` | 每日 1 次 | ✅ v0.3 已实现（当日概念标签用） |
| 6 | **概念成分股** `clist fs=b:BKxxxx` | 题材 → 成分股映射 | 成分股代码 | 按需/每日 | ✅ v0.3 已实现（best-effort，仅当日） |
| 7 | **个股资金流** | 主力/超大单/大单净流入 | 主力净流入/超大单/大单 | 每日 1 次（炸板股） | ✅ v0.3 已实现：**当日=clist 批量**（f62/f66/f72）；**历史=fflow 单股**（f52/f56/f55） |
| 8 | **概念资金流** | 题材整体资金进出 | 板块主力净流入 f62 | 每日 1 次 | ✅ v0.3 已实现（`fetch_concept_boards`） |
| 9 | **个股行情/分时**（可选） | 封板时间补充、次日对照 | 分时价量 | 后续 | ⬜ 待实现 |

### 已验证的真实参数（2026-08-07 实测）

- **池子**：`getTopicZTPool?ut=7eea3edcaed734bea9cbfc24409ed989&dpt=wz.ztzt&Pageindex=0&pagesize=300&sort=fbt:asc&date=YYYYMMDD`（`date` 支持历史交易日）→ `data.pool[]`
- **单股历史资金流**：`push2his.eastmoney.com/api/qt/stock/fflow/kline/get?secid={1|0}.{code}&fields2=f51..f65` → klines 每行 `日期,主力净流入,小单,中单,大单,超大单`
- **全市场资金流（当日）**：`clist?fid=f62&fs=m:0+t:6,m:0+t:80,m:1+t:2,m:1+t:23&fields=f12,f14,f62,f66,f72`（f62=主力 f66=超大单 f72=大单；已核对超大单+大单=主力）

> 具体 URL 与参数以 `src/daily_review/data/eastmoney_pool.py` 内实现为准。**每次上线前需验证接口可用性。**

## 二、多日数据需求

以下分析依赖**跨日时序**，需把每日 #1/#2/#5/#7 结果落盘到 `data/{trade_date}/`：
- 连板晋级率：需要昨日涨停清单 → 今日对照
- 题材运行周期：需要题材成员数的**逐日变化**（启动=首现增多 / 发酵=扩散 / 高潮=达峰 / 退潮=减少）

实现时对历史日期做**回补抓取**（如一次性抓近 30 个交易日），并建立本地日线缓存。

## 三、风险与对策清单

| 风险 | 对策 | 触发说明 |
|---|---|---|
| 接口字段变更 | 字段映射集中 + 解析后 schema 校验 | 解析失败即报警 |
| 频率限制/IP 封锁 | 频率控制、UA 伪装、错误码重试、本地缓存 | 出现 403/空返回 |
| 盘中/停牌数据不稳 | 仅收盘后采集 | 15:00 后 |
| 新股/次新无题材 | 清洗规则集中定义 | 概念字段为空 |
| 交易日期（节假日/调休） | 交易日历本地化（东财 `get_trade_dates`） | 用于确定最近交易日 |

## 四、采集层模块现状（v0.3）

```
src/daily_review/data/
├── http_client.py        # 请求封装：UA/超时/重试/指数退避
├── eastmoney.py          # 东财 K 线（v0.2）
├── eastmoney_pool.py     # 池子/资金流/概念板块（v0.3）：
│                          #   主机轮换 + ≥1s 节流 + 连续失败熔断
├── repo.py               # data/{trade_date}/ CSV 落盘与读取（按交易日）
└── sina.py               # 新浪实时行情（v0.2）
```

交易日探测：`eastmoney_pool.resolve_recent_trade_dates` 拉涨停池判空回退，**免维护交易日历**。
历史日期资金流依赖 `push2his`（偶发限流，`push2delay` 兜底但仅当日）；当日资金流走 clist 批量（1 请求）。
