# 版本管理约定（Git 工作流）

> 目的：**每轮对话结束自动提交 + 标注版本 + 上传**，保证工程可回溯、可复现。
> 版本号由 AI 根据**工作量和改动大小**标注（用户已授权）。
> 当前：远程 `origin`（`mowanyy/a-share-daily-review`，public）已配置，push 走 `git push origin main --tags`（直连需加速器，或用 `-c http.proxy= -c https.proxy=` 绕过全局代理）。

---

## 1. 版本号规则（语义化版本 MAJOR.MINOR.PATCH）

项目处于 `0.x` 开发期。版本推进规则：

| 改动类型 | 例子 | 版本推进 |
|---|---|---|
| **PATCH（小改）** | 修 bug、补文档、环境同步、测试调整、格式化 | `0.2.0 → 0.2.1` |
| **MINOR（新功能）** | 新增涨停池/炸板池爬虫、指标计算、LLM 报告、问答模式 | `0.2.x → 0.3.0` |
| **MAJOR（架构级）** | 架构重构、或正式发布 | `0.x → 1.0.0` |

- 提交时同步更新 `pyproject.toml` 的 `version` 字段，二者保持一致
- 历史版本：
  - `v0.1.0` — 需求分析 + Prompt 工程骨架（文档阶段，未单独提交）
  - `v0.2.0` — 数据采集层起步（东财 K 线 + 新浪实时行情）+ 环境同步（首次入库）
  - `v0.3.0` — 端到端复盘：东财池子/资金流/板块采集 → 连板梯队/题材/炸板指标 → DeepSeek 报告
  - `v0.4.0` — 龙虎榜游资分析（东财龙虎榜 + 买卖席位 + 知名游资识别）
  - `v0.5.0` — 情绪温度（五维加权 + 情绪周期阶段），报告七章
  - `v0.6.0` — 数据看板（近 10 日趋势图表单文件 HTML + LLM 多日解读）
  - `v0.7.0` — 交互问答：RAG 短线知识库（混合检索 + 持续更新）+ 数据工具 function-calling
  - `v0.8.0` — Flask 全套工作台（战法管理 / 跑复盘看报告与次日预案 / 问答 / 数据看板）+ 个人战法 UI 上传（落盘 gitignored `data/strategies/`，不写死进项目）→ 驱动次日预案
  - `v0.9.0` — 图形启动器：`启动.bat` / 桌面快捷方式「每日复盘」打开 tkinter 窗口，一键启动 Web 工作台 / 跑复盘 / 数据看板 / 交互问答（launcher.py 纯核心 + launcher_gui.py 窗口 + `launch` CLI 子命令 + `--dry-run` 自检）
  - `v0.10.0` — 修复 Web 数据看板「不显示」+ 内容增强放一页：根因 `/api/dashboard/view` 每次请求重新联网采集约 2 分钟导致 iframe 空白；新增进程内 `DashboardCache`（按 交易日期+N+no_llm 缓存，最多 16 条逐出最旧，只缓存成功）+ 复用已生成 `output/{date}_看板.html`（默认 10 日）+ 自包含错误兜底页 + 保鲜规则（历史定稿常新 / 今日盘中 10 分钟 / 收盘后须 15:00 后生成）；新增「近 N 日趋势摘要表」「情绪温度成分拆解」面板，iframe 自适应高度与页面融为一体，LLM 解读配置 key 默认开启，review 页加「查看该日看板」跳转，新增 `/api/config/llm`
  - `v0.11.0` — 多模型协作热点复盘：概念板块行情采集扩展领涨股字段（f128/f140/f136 实测，无则回退 4 列）；pipeline 新增「仅当日」概念板块可选块（clist 实时快照，历史日期硬守卫不采集、不落脏缓存）+ compute 透传 Top-12 概念榜；撰写复盘时由独立热点模型（模型 B，`module.hotspot` prompt，`HOTSPOT_MODEL` 可选、默认回落主模型）基于概念板块+当日题材提炼当日 2-4 条热点主线简报，注入主分析师（模型 A）「一总览/四题材/七次日预案」三章节撰写（不新增章节、`generate_report` 签名不变）；热点调用失败降级确定性 Top-N，无概念数据逐字节保持旧行为（7 次调用不变）

## 2. 提交流程（每轮对话结束时执行）

```bash
# 1. 提交前检查
git status --short          # 确认改动范围、确认无 data/*.csv 入库
python -m pytest            # 测试通过再提交

# 2. 暂存与提交
git add -A
git commit -m "<type>(<scope>): <简述>"

# 3. 标注版本（按第 1 节规则）
git tag v0.x.y

# 4. 上传（远程配置后）
git push origin main --tags
```

## 3. Commit 消息格式

```
<type>(<scope>): <简述>

示例：
  feat(data): 实现东财涨停池接口
  docs(env): 同步开发环境文档
  fix(cli): 修复控制台中文编码
  chore: 初始化 git 仓库
```

| type | 含义 |
|---|---|
| `feat` | 新功能 |
| `fix` | 修复 |
| `docs` | 文档 |
| `refactor` | 重构（不改行为） |
| `test` | 测试 |
| `chore` | 构建/杂项/初始化 |

| scope（常用） | 含义 |
|---|---|
| `data` | 数据采集层 |
| `prompts` | Prompt 工程 |
| `docs` | 文档 |
| `cli` | 命令行 |
| `env` | 环境/依赖 |
| `(空)` | 全局性改动 |

## 4. 入库 / 不入库

- **入库**：`docs/` `prompts/` `src/` `tests/` `knowledge/` `requirements*.txt` `pyproject.toml` `.gitignore` `.gitattributes` `CLAUDE.md` `README.md`
- **不入库**（`.gitignore`）：`data/*/` `output/*/` `*.csv` `models/` `.kb_cache/` 缓存与虚拟环境目录
