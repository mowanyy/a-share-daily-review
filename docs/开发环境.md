# 开发环境（Windows）

> 目的：记录本项目**已确认可用的运行环境**，供版本控制后新机器/协作者一键复现。
> 版本控制配套文件：`requirements.txt`（运行时）、`requirements-dev.txt`（开发）。
> 最后核验：2026-08-06（在 mowan_dm 下实测通过）

---

## 1. 环境一览

| 项 | 值 |
|---|---|
| 操作系统 | Windows 11 Home（build 26200） |
| 解释器 | **mowan_dm** conda 环境 · Python **3.14.6**（conda-forge，64 位） |
| 环境路径 | `E:\conda_envs\envs\mowan_dm` |
| conda 位置 | `C:\ProgramData\Anaconda3\Scripts\conda.exe`（base） |
| pandas | 3.0.3 |
| numpy | 2.5.1 |
| requests | 2.34.2 |
| pytest | 9.1.1（开发） |

> ⚠️ 本项目代码使用 **Python 3.10+ 语法**（`X | None`、`list[T]`），**不可**在 Python 3.8 环境运行。
> 另一环境 `E:\zhouquan_dm\zhouquan_dm`（Python 3.8.8）不满足要求，勿混用。

## 2. 激活环境

```bash
# 方式一：conda 激活（mowan_dm 已注册到 conda）
conda activate mowan_dm
python --version   # 应输出 Python 3.14.6

# 方式二：直接用完整路径（不依赖激活，最稳）
E:/conda_envs/envs/mowan_dm/python.exe --version
```

## 3. 运行本项目（项目根目录执行）

包采用 `src/` 布局，运行前需把 `src` 加入模块搜索路径（`PYTHONPATH`）。

```bash
# Git Bash（本项目常用）
cd "e:/workspace/AI应用工具/每日复盘"
PYTHONPATH=src "E:/conda_envs/envs/mowan_dm/python.exe" -m daily_review kline --code 600000 --lmt 30
PYTHONPATH=src "E:/conda_envs/envs/mowan_dm/python.exe" -m daily_review realtime --codes 600601,002398,600789
```

```powershell
# PowerShell（激活环境后）
cd "e:\workspace\AI应用工具\每日复盘"
$env:PYTHONPATH = "src"
python -m daily_review kline --code 600000 --lmt 30
```

> 可选：`python -m pip install -e .`（装进 mowan_dm）后即可免 `PYTHONPATH` 直接 `daily-review` 命令或 `python -m daily_review`。

## 4. 安装 / 复现依赖

```bash
# 在 Python 3.14 环境内执行（本环境已就绪，无需重装）
"E:/conda_envs/envs/mowan_dm/python.exe" -m pip install -r requirements.txt -r requirements-dev.txt
```

`requirements.txt` 已按 mowan_dm 实测版本锁定。**安装只允许进入 mowan_dm 环境或本项目文件夹**，不得装到其他位置（用户约定）。

## 5. Windows 注意事项

| 坑 | 说明 |
|---|---|
| **GBK 控制台编码** | 终端 print 含 emoji（如 ✅）会报 `UnicodeEncodeError`。CLI 已改用 GBK 安全字符；自己写脚本时避免非 GBK 字符 |
| **中文显示乱码** | Git Bash 显示中文可能 mojibake，属显示层问题，数据本身 UTF-8 无损（CSV 用 `utf-8-sig` 落盘，Excel 可直接打开） |
| **路径分隔符** | 代码内一律用 `pathlib.Path`；命令行用 `/` 或 `\` 均可 |
| **外部数据接口** | 东财/新浪接口需联网；UA 与 Referer 头已在 `data/http_client.py` 统一处理 |

## 6. 版本控制建议

- 加入版本库：`docs/`、`prompts/`、`src/`、`tests/`、`requirements*.txt`、`pyproject.toml`
- 不入库（已在 `.gitignore`）：`data/*/`、`output/*/`、`*.csv`、`__pycache__/`、`.pytest_cache/`
- 新机器复现：创建 Python 3.14 conda 环境 → `pip install -r requirements.txt -r requirements-dev.txt` → `python -m pytest` 应全部通过
