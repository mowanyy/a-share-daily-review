# 回测数据说明（v0.38.4）

> 为策略回测整理的数据资产说明：**当前已有什么、格式如何、缺什么、怎么补**。
> 数据全部落盘在 `data/` 下（`data/*/` 已 gitignore 不入库）。

## 一、已有什么（截至 v0.38.4）

### 1. 涨跌停池（回测核心：哪些股哪天涨停/炸板）

| 数据 | 路径 | 覆盖 | 字段 |
|---|---|---|---|
| 涨停池 | `data/{date}/zt_pool.csv` | **20260722~20260911**（28+ 个交易日） | `trade_date,code,name,lb_num,first_limit_time,last_limit_time,open_times,seal_amount,turnover,amount,industry` |
| 炸板池 | `data/{date}/zb_pool.csv` | 同涨停池 | `trade_date,code,name,break_times,first_seal_time,up_pct,industry` |
| 跌停池 | `data/{date}/dt_pool.csv` | **空**（东财 getTopicDTPool 接口不返回数据，可用 K 线 `pct_change<=-9.9%` 推导） | — |

> **边界**：东财 `getTopicZTPool` 接口只提供近期（约 1-2 个月）涨停池，20260722 之前的历史请求返回空——**7/22 前的涨停/炸板名单无法从东财直接获取**（已探测 20241001~20260722 全部 pool=0，非限流，是接口数据深度限制）。

### 2. 龙虎榜（回测游资/席位因子）

| 数据 | 路径 | 覆盖 |
|---|---|---|
| 龙虎榜每日 | `data/{date}/lhb_daily.csv` | 7/22~9/10 大部分（补齐中，datacenter-web 历史可用） |
| 买卖席位 | `data/{date}/lhb_seats.csv` | 同上 |

字段见 `src/daily_review/data/eastmoney_lhb.py`（21+11 列：净买额/买入卖出席位/上榜原因等）。

### 3. 个股日 K（回测价格/涨跌序列）

| 数据 | 路径 | 覆盖 |
|---|---|---|
| 涨停股历史日K | `data/kline/{code}.csv` | **进行中**：7/22~9/11 全部涨停股去重 1287 只，已拉取成功 23 只，每只 1000 根（约 4 年，含 trade_date/open/close/high/low/volume/amount/pct_change/turnover 等） |

> **今日网络限制**：东财 `push2his` K 线域名当前连接被断（`RemoteDisconnected`，非代码问题）→ 批量拉取中断。**网络恢复后重跑**：
> ```bash
> "E:/conda_envs/envs/mowan_dm/python.exe" -u -m tools.backfill_kline --lmt 1000 --sleep 1.2
> ```
> 已成功的文件自动跳过（幂等）。

### 4. 其它

- `data/cache/industry_map.csv`：全市场 代码→行业 映射（7 天 TTL）
- `data/{date}/concept_boards.csv`：概念板块行情（仅当日实时快照，历史无）
- `data/{date}/moneyflow_zb.csv`：炸板股资金流（仅部分日期，历史单股 fflow 接口受限）
- `data/trade_calendar.csv`：权威交易日历（1300+ 交易日）

## 二、回测可用性评估

| 回测需求 | 现状 | 建议 |
|---|---|---|
| 涨停名单（7/22 后） | ✅ 完整 | 直接用 zt_pool.csv |
| 涨停连板数 | ✅ `lb_num` 字段 | 也可由 K 线连续涨停推导交叉验证 |
| 涨停后 N 日表现 | ⚠️ 需 K 线 | 网络恢复后跑 backfill_kline 补齐 |
| 炸板/开板次数 | ✅ zb_pool + open_times | 可直接用 |
| 龙虎榜净买 | ✅ lhb_daily | 补齐中 |
| 情绪温度/晋级率等指标 | ✅ 可重算 | 复用 `analysis/` 计算函数 |
| 7/22 前涨停名单 | ❌ 数据源无 | 若必须：需换数据源（akshare/tushare 等，超出本仓库「东财接口」边界）或人工/第三方购买 |

## 三、补数工具

```bash
# 1. 批量预生成/补采看板（zt/zb 缺则联网补，看板落盘 output/{date}_看板.html）
python -u -m tools.backfill_dashboards --days 60 --skip-data
# 2. 补指定日期（数据 + 看板）
python -u -m tools.backfill_dashboards --dates 20260910,20260911
# 3. 补龙虎榜/资金流（datacenter 历史可用；--fill-extra 慢，跑多少算多少）
python -u -m tools.backfill_dashboards --dates <列表> --fill-extra --skip-dashboards
# 4. 批量拉涨停股历史日K（回测价格数据；push2his 网络恢复后跑）
python -u -m tools.backfill_kline --lmt 1000 --sleep 1.2
# 5. 清理非交易日污染目录（仅删明确休市日）
python -u -m tools.backfill_dashboards --clean-junk
```

## 四、K 线数据格式（data/kline/{code}.csv）

```csv
trade_date,open,close,high,low,volume,amount,amplitude,pct_change,change,turnover
2026-07-31,9.59,9.51,9.59,9.28,1382638,1299502653.0,3.19,-2.06,-0.2,0.42
```
- 日线（klt=101）、默认不复权（fqt=0，`pct_change` 为真实涨跌幅，回测基于涨跌幅不受复权影响；如需前复权价格可 `--fqt 1` 重拉）
- 代码 6 位零填充（`600000`），股票池 = 7/22~9/11 全部涨停股去重

## 五、网络恢复后的完整补数步骤

1. `tools/backfill_kline.py`（K 线，push2his 恢复即跑，1287 只约 25-40 分钟 @1.2s 间隔）
2. `tools/backfill_dashboards.py --fill-extra`（龙虎榜剩余，datacenter 可用）
3. 回测脚本直接读 `data/zt_pool/` `data/kline/` 两个目录即可。
